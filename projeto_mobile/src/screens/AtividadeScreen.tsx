import AtividadeAvaliativa3 from "../layouts/AtividadeAvaliativa3";
import { AtividadeProps } from "../types";

const AtividadeScreen = ({navigation, route}: AtividadeProps) => {
    return (

        <AtividadeAvaliativa3 navigation={navigation} route={route} />

    );
};

export default AtividadeScreen;